#include "main.h"

/**
 * add - takes two integer arguments and adds them together.
 * @x: first integer
 * @y: second integer
 *
 * Return: the result;
 */
int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
